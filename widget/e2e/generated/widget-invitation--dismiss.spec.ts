// Auto-generated by @afixt/usecase-runner from invitation--dismiss.uc.yaml
// Do not edit manually — changes will be overwritten on next generation.

import { test, expect } from '@playwright/test';

test.describe('Visitor dismisses the proactive invitation', () => {
  test.describe.configure({ mode: 'serial' });

  test('widget-invitation--dismiss: Visitor dismisses the proactive invitation (extension)', async ({ page }) => {
    // Preconditions:
    //   - At least one support agent is online and available for the tenant.
    //   - The widget is closed and showing the invitation flourish.

    // Step 1: Access start location
    await page.goto('http://localhost:5175/');

    // Step 2: Locate region "Chat invitation" (invitation--dismiss.uc.yaml:1)
    await expect(page.getByRole('region', { name: 'Chat invitation' })).toBeVisible();

    // Step 3: Locate button "Dismiss chat invitation" (invitation--dismiss.uc.yaml:2)
    await expect(page.getByRole('button', { name: 'Dismiss chat invitation' })).toBeVisible();

    // Step 4: Focus button "Dismiss chat invitation" (invitation--dismiss.uc.yaml:3)
    const step4_el = page.getByRole('button', { name: 'Dismiss chat invitation' });
    await step4_el.focus();
    await expect(step4_el).toBeFocused();

    // Step 5: Activate button "Dismiss chat invitation" (invitation--dismiss.uc.yaml:4)
    await page.getByRole('button', { name: 'Dismiss chat invitation' }).click();

    // Step 6: Locate button "Chat with support" (invitation--dismiss.uc.yaml:5)
    await expect(page.getByRole('button', { name: 'Chat with support' })).toBeVisible();

  });
});
