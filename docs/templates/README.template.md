# &lt;package name&gt;

One-line description of what this package does.

## Install

```sh
npm install &lt;package&gt;
```

## Usage

```ts
// minimal example
```

## Scripts

| Script         | Purpose              |
| -------------- | -------------------- |
| `npm run dev`  | &lt;what it does&gt; |
| `npm test`     | &lt;what it does&gt; |
| `npm run lint` | &lt;what it does&gt; |

## Contributing

See the repo-root README and `docs/adr/` for architectural decisions. All
changes must pass `npm run check:all` locally before being pushed.

## License

&lt;license&gt;
