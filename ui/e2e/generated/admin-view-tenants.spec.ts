// Auto-generated by @afixt/usecase-runner from view-tenants.uc.yaml
// Do not edit manually — changes will be overwritten on next generation.

import { test, expect } from '@playwright/test';

test.describe('Super admin views the tenants list', () => {
  test.describe.configure({ mode: 'serial' });

  test('admin-view-tenants: Super admin views the tenants list (positive)', async ({ page }) => {
    // Preconditions:
    //   - A super_admin user is already authenticated.

    // Step 1: Access start location
    await page.goto('http://localhost:5174/admin/tenants');

    // Step 2: Locate heading "Tenants" (view-tenants.uc.yaml:1)
    await expect(page.getByRole('heading', { name: 'Tenants' })).toBeVisible();

    // Step 3: Locate button "Create tenant" (view-tenants.uc.yaml:2)
    await expect(page.getByRole('button', { name: 'Create tenant' })).toBeVisible();

  });
});
