// Auto-generated by @afixt/usecase-runner from view-users.uc.yaml
// Do not edit manually — changes will be overwritten on next generation.

import { test, expect } from '@playwright/test';

test.describe('Admin views the users list with tenant filter', () => {
  test.describe.configure({ mode: 'serial' });

  test('admin-view-users: Admin views the users list with tenant filter (positive)', async ({ page }) => {
    // Preconditions:
    //   - An admin or super_admin is authenticated.

    // Step 1: Access start location
    await page.goto('http://localhost:5174/admin/users');

    // Step 2: Locate heading "Users" (view-users.uc.yaml:1)
    await expect(page.getByRole('heading', { name: 'Users' })).toBeVisible();

    // Step 3: Locate select "Filter by tenant" (view-users.uc.yaml:2)
    await expect(page.getByRole('combobox', { name: 'Filter by tenant' })).toBeVisible();

  });
});
